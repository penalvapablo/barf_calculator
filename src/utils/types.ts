export type PetData = {
  name: string;
  age: number;
  weight: number;
  months: number;
  state: "nutered" | "unnutered";
};

export type Ingredients = {
  bone: number;
  meat: number;
  viscera: number;
  fiber: number;
};
